using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using YProgramStudio.LabelsDesigner.Model;

namespace YProgramStudio.LabelsDesigner.Tests
{
	[TestClass]
	public class TestXmlLabel
	{
		//[TestMethod]
		//public void InitTestCase()
		//{
		//	Db.Init();
		//	var papers = Db.Papers;
		//	var categories = Db.Categories;
		//	var venders = Db.Vendors;
		//	var templates = Db.Templates;

		//	Assert.IsTrue(papers.Count > 0);
		//	Assert.IsTrue(categories.Count > 0);
		//	Assert.IsTrue(venders.Count > 0);
		//	Assert.IsTrue(templates.Count > 0);
		//}
	}
}
